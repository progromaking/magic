
&НаСервере 
Функция ПолучитьПолноеИмяФормы(ИмяФормы)

    СимволТочка = ".";
    ПозицияТочки = СтрДлина(ЭтаФорма.ИмяФормы);
    Пока Сред(ЭтаФорма.ИмяФормы, ПозицияТочки, 1) <> СимволТочка Цикл ПозицияТочки = ПозицияТочки - 1; КонецЦикла; //
    Возврат Лев(ЭтаФорма.ИмяФормы, ПозицияТочки) + ИмяФормы;

КонецФункции


&НаСервере
Процедура ОткрытьФормуСПараметрамиНаСервере()
	ПолноеИмяФормы = ПолучитьПолноеИмяФормы("ФормаОткрываемая");
	
	МойОбъект = РеквизитФормыВЗначение("Объект");
	Форма = МойОбъект.ОткрытьИВернутьФорму(ПолноеИмяФормы, ЭтаФорма);
	//ПараметрыФормы = Новый Структура();
	//Форма = ОткрытьФорму(ПолноеИмяФормы, ПараметрыФормы);
	
	//Форма = ОткрытьФорму(ПолноеИмяФормы, ПараметрыФормы, ЭтаФорма);
	Форма.Тест();
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуСПараметрами(Команда)
	ОткрытьФормуСПараметрамиНаСервере();
КонецПроцедуры
