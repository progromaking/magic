

&НаСервере
Процедура СоздатьТаблицуНаФорме()
	
	Макет = РеквизитФормыВЗначение("Объект").ПолучитьМакет("СлучайныеДанныеДляПримера");
	ТестовыеДанные = Обработки.Тестирование_ГенераторДанных.Создать().СоздатьДанныеПоТабличномуДокументу(Макет);
	
	МояТаблица = ТестовыеДанные.Таблица1;
	МояТаблица.Колонки[1].Заголовок = "тест";
	
	ПрограммнаяРаботаСИнтерфейсом.ОбновитьТаблицуНаФормеПоТаблицеЗначений(ЭтаФорма, "Таблица1", МояТаблица);
	
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	СоздатьТаблицуНаФорме();
КонецПроцедуры
